databases:
  - name: caresync-db
    databaseName: caresync
    user: admin
    plan: free

services:
  - type: web
    name: caresync
    env: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: caresync-db
          property: connectionString