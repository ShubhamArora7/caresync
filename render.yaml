services:
  - type: web
    name: caresync
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "python manage.py migrate && python create_superuser.py && gunicorn caresync_project.wsgi"